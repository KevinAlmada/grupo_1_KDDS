<!DOCTYPE html>
<html lang="en">
    <%- include('partials/head.ejs') %>
    <link rel="stylesheet" href="/stylesheets/adminIndex.css"> 
<body>
    <nav class="navAdmin">
        <img class="logoAdmin" src="/images/admin/LOGO.png" alt="logo">
        <ul>
            <li><a class="btn-newp" href="#">Productos</a></li>
            <li><a class="btn-newp" href="#">Usuarios</a></li>
            <li><a class="btn-newp" href="/admin/newproduct">Agregar</a></li>
        </ul>
    </nav>
    <section class="admin-section">
        <table class="adminP-table">
            <thead>
              <tr>
                <th>id</th>
                <th>Nombre</th>
                <th>Categoría</th>
                
                <th>Precio</th>
                <th>Descuento</th>
                <th>Editar</th>
                <th>Eliminar</th>
              </tr>
            </thead>
            <tbody>
                <% productdb.forEach(product => { %>
                  <tr class="tablerow-admin">
                      <td><%= product.id %> </td>
                      <td><%= product.name %></td>
                      <td><%= product.category %></td>
                      <td><%= product.price %></td>
                      <td><%= product.discount %></td>
                      <td><a href="/admin/changeProduct/<%=product.id %> "> <i class="fas fa-user-edit"></i></a></td>
                      <td><form action="/admin/deleteProduct/<%= product.id %>?_method=DELETE" method="POST">
                        <button class="delete-product" type="submit"><i class="fas fa-trash"></i></button>
                    </form></td>
                    </tr>
                <% }) %> 
            </tbody>
          </table>
    </section>
    <div class="btn-new-container">
        <a class="btn-newp" href="/admin/newproduct">Agregar Producto</a>
    </div>
</body>
</html>